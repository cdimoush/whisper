---
allowed-tools: Bash(python:*), Bash(uv:*)
description: Show status of audio files in queue
---

Run the queue status script to show pending audio files:

```bash
uv run python scripts/queue_status.py
```

Display the output to the user.
